import * as React from 'react';
import { Unit } from '../types';
interface Props {
    x?: number;
    y?: number;
    width: number;
    height: number;
    unit: Unit;
}
export declare const UnitComponent: React.FC<Props>;
export {};
