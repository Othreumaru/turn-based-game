{
  "name": "bootstrap",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "start": "cd apps/the-game && yarn start",
    "build:shared": "tsc --build",
    "build:app": "cd apps/the-game && yarn build",
    "build": "yarn build:shared && yarn build:app"
  },
  "workspaces": [
    "apps/*",
    "shared/*"
  ],
  "license": "",
  "devDependencies": {
    "@types/node": "14.14.6",
    "prettier": "^2.1.2",
    "typescript": "^4.0.5"
  }
}
